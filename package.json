{
  "name": "audri.us",
  "description": "audri.us website",
  "version": "3.1.0",
  "private": true,
  "author": "Audrius Lubys",
  "scripts": {
    "build": "node ./build src/index.pug src/static dist",
    "start": "npm run build && http-server dist -o"
  },
  "devDependencies": {
    "dotenv": "^14.3.0",
    "http-server": "^14.1.0",
    "pug": "^3.0.2",
    "sass": "^1.49.0"
  }
}
